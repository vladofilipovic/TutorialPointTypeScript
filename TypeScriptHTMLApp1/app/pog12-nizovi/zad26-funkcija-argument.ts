var names: string[] = new Array("Mary", "Tom", "Jack", "Jill")

function prikaz(arr_names: string[]) {
    for (var i = 0; i < arr_names.length; i++) {
        console.log(names[i])
    }
}
prikaz(names)